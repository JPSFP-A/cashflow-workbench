{
  "name": "cashflow-workbench",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-user cashflow mapping workbench with Git-backed CSV/JSON storage.",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
